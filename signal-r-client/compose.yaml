version: '3.8'

services:
  server:
    build:
      context: .
    environment:
      NODE_ENV: development
    ports:
      - '3000:3000'
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: ['yarn', 'start']
